let initialState = {

}

const headerReducer = (state = initialState, action) => {
    switch (action.type) {

        default: {
            return state
        }
    }
}

//ACTION CREATORS








//THUNK CREATORS





export default headerReducer